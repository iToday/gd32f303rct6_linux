# ⚡ F5 调试 - 快速修复（3 步）

## 🔴 问题
按 F5 启动调试失败 → DEBUG CONSOLE 报错

## 🟢 原因
**缺少 `arm-none-eabi-gdb` 调试器工具**

## ✅ 解决（3 步）

### 1️⃣ 安装 GDB
```bash
sudo apt-get update && sudo apt-get install -y gdb-multiarch
```

### 2️⃣ 创建链接
```bash
sudo ln -sf /usr/bin/gdb-multiarch /usr/bin/arm-none-eabi-gdb
```

### 3️⃣ 验证安装
```bash
arm-none-eabi-gdb --version
# 应该显示版本信息 ✅
```

## 🚀 现在开始
按 **F5** 启动调试！

---

**时间**：< 2 分钟
**难度**：简单
**结果**：调试完全就绪 ✅
